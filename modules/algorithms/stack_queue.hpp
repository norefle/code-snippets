#pragma once

#include <string>

namespace module {

/// Builds the queue on top of two stacks and verifies its properties.
std::string solveQueueAsStacks();

/// Builds the stack on top of two queues and verifies its properties.
std::string solveStackAsQueues();

} // namespace module
